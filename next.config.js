/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['hmchile.vtexassets.com', 'img.ltwebstatic.com', 'cdn.shopify.com', 'via.placeholder.com'],
  },
}

module.exports = nextConfig
